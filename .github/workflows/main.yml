name: Mirror

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: mirror
      run: |
          set -x
          cd $GITHUB_WORKSPACE
          echo "GITHUB_REPOSITORY=$GITHUB_REPOSITORY"
          mkdir ~/.ssh
          ssh-keyscan git.code.sf.net > ~/.ssh/known_hosts
          export DISPLAY=:0
          export SSH_PASSWORD=jirkami01x
          export SSH_ASKPASS=ci/askpass.sh
          export GIT_ASKPASS=ci/askpass.sh
          export GIT_SSH_COMMAND="setsid ssh"
          git checkout master
          git config credential.username malakj
          git remote set-url origin ssh://malakj@git.code.sf.net/p/openwatcom/open-watcom-v2-test
          git push -v
      env:
        OW_SF_TOKEN1: ${{ secrets.OW_SF_TOKEN1 }}
        OW_SF_TOKEN2: ${{ secrets.OW_SF_TOKEN2 }}
