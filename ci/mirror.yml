steps:
- script: |
    git checkout master
    export GIT_ASKPASS=ci/askpass.sh
    export SSH_PASSWORD=$(owsftoken2)
    git remote set-url --push origin https://$(owsftoken1)@git.code.sf.net/p/$(owsfproject)
    git push -v
  displayName: "Mirror to SF"
