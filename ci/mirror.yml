steps:
- script: |
    git checkout master
    export SSH_ASKPASS=ci/askpass.sh
    git remote set-url --push origin ssh://$(owsftoken1)@git.code.sf.net/p/$(owsfproject)
    git push -v
  displayName: "Mirror to SF"
