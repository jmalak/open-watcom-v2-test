parameters:
  owarch: $(owarch)

steps:
    - task: DownloadPipelineArtifact@2
      displayName: Load Artifacts bld-setupgui-${{ parameters.owarch }}
      inputs: 
        buildType: 'current'
        artifactName: bld-setupgui-${{ parameters.owarch }}
    - template: archivel.yml
      parameters: 
        owfilepath: bld/setupgui
        owtitle: Extract Artifacts bld-setupgui-${{ parameters.owarch }}
