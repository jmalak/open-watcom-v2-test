parameters:
  owartifname: ''

steps:
    - task: DownloadPipelineArtifact@2
      displayName: "Load Artifact ${{ parameters.owartifname }}"
      inputs: 
        buildType:    'current'
        artifactName: "${{ parameters.owartifname }}"
        targetPath:   rel